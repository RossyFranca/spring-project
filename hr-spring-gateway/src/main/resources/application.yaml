server:
  port: 8765
spring:
  application:
    name: hr-spring-gateway
  cloud:
    gateway:
      routes:
        - id: hr-worker
          uri: lb://hr-worker
          predicates:
            - Path=/workers/**
        - id: hr-payroll
          uri: lb://hr-payroll
          predicates:
            - Path=/payments/**
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


